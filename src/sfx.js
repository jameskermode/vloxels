// sfx.js — sound hook STUB. v1 has no sound (a deliberate non-goal), but the
// game calls these no-ops at the obvious moments so adding sound later is just
// filling them in (e.g. load an <audio> or use the Web Audio API here). Keeping
// the call sites in place now means no hunting later.

export const sfx = {
  coin() {},
  jump() {},
  win() {},
  respawn() {},
};
